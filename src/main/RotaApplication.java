package main;

import controller.Rota;

//This class is executed to allow the program to run.
public class RotaApplication {

	public static void main(String[] args) {
		Rota r = new Rota(); // Creates a new instance of the Rota class.
		r.runRota(); // Executes the runRota() method from the Rota class.
	}

}
